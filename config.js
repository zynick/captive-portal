'use strict';

const {
  AD_HOST = '',
  AD_KEY = '',
  HOST = '',
  MONGO = '',
  NODE_ENV = 'development',
  PORT = 3000
} = process.env;

module.exports = {
  AD_HOST,
  AD_KEY,
  HOST,
  MONGO,
  NODE_ENV,
  PORT
};
